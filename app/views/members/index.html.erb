<h1>WELCOME</h1>
<p>Support Org Ticket Workflow</p>

<br>

<strong> Select a Member:
<% @members.each do |member| %>
<li>
  <%= link_to member.name, member %>
  <% if current_member.admin? && !current_member?(member) %>
    | <%= link_to "delete", member, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>

<% end %>

<% if !logged_in? %>
 <h5><%= link_to "Not here? Click here to create", new_member_path %></h5>
<% end %>

<br>
<br>
 <strong>Find a Ticket!
<%= form_tag(tickets_path, :method => "get", id: "search-form") do %>
    <%= text_field_tag :search, params[:search], placeholder: "Search Tickets" %>
    <%= submit_tag "Search", :name => nil %>
<% end %>

